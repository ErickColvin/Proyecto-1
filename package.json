{
  "name": "inventario-digital-platform",
  "version": "1.0.0",
  "description": "Plataforma de gestión de inventario con MongoDB Atlas",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd my-digital-platform && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../my-digital-platform && npm install"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "concurrently": "^8.2.2",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.4"
  }
}
